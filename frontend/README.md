### File hướng dẫn cài đặt để chạy Frontend: 
1. `npm install` (Gói cơ bản)
2. Cài đặt các thư viện bổ sung: `npm install react-router-dom lucide-react axios`
3. Cài đặt Tailwind CSS: `npm install -D tailwindcss postcss autoprefixer`

4. Lệnh chạy dự án `npm run dev`.

